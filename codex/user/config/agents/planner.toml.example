# Example role-specific Codex config for planning work.
# Install as ~/.codex/agents/planner.toml when enabling the planner role.

model = "gpt-5.6"
model_reasoning_effort = "medium"
personality = "sol"
