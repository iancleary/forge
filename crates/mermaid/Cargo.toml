[package]
name = "mermaid"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
cli-core = { path = "../cli-core" }
serde = { version = "1", features = ["derive"] }
